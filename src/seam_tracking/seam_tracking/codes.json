["def fn(x, y):\n    if len(x) == 0 or len(y) == 0 or len(x) != len(y):\n        return False, 0., 0.\n    else:\n        return True, x[0] * 1., y[0] * 1.\n", "def fn(x, y):\n    if len(x) == 0 or len(y) == 0 or len(x) != len(y):\n        return False, 0., 0.\n    else:\n        return True, x[-1] * 1., y[-1] * 1.\n", "from numpy.polynomial.polynomial import polyfit\n\ndef fn(x, y):\n    return Calculate(x, y, 50, 5)\n\ndef Calculate(x, y, num, delta):\n    index = y.index(max(y))\n    if index - num < 0 and index + num > len(y):\n        return False, 0., 0.\n    b1, m1 = polyfit(x[index - num : index - delta], y[index - num : index - delta], 1)\n    b2, m2 = polyfit(x[index + delta : index + num], y[index + delta : index + num], 1)\n    try:\n        px = (b2 - b1) / (m2 - m1)\n        py = px * m1 + b1\n    except:\n        return False, 0., 0.\n    else:\n        return True, px, py", "from numpy.polynomial.polynomial import polyfit\n\ndef fn(x, y):\n    return Calculate(x, y, 50, 5)\n\ndef Calculate(x, y, num, delta):\n    index = y.index(min(y))\n    if index - num < 0 and index + num > len(y):\n        return False, 0., 0.\n    b1, m1 = polyfit(x[index - num : index - delta], y[index - num : index - delta], 1)\n    b2, m2 = polyfit(x[index + delta : index + num], y[index + delta : index + num], 1)\n    try:\n        px = (b2 - b1) / (m2 - m1)\n        py = px * m1 + b1\n    except:\n        return False, 0., 0.\n    else:\n        return True, px, py"]
